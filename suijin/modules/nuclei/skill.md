# Nuclei

> ⚠️ **LONG-RUNNING** — always use `"background": true`

Template-based vulnerability scanner with 8000+ community templates.

```json
{"tool": "nuclei_scan", "args": {"target": "https://target.com", "templates": "cve,exposures,misconfig", "background": true}}
```

Template tags: `cve`, `exposures`, `misconfig`, `panel`, `tech`, `fuzzing`, `workflow`, `osint`, `dast`, `token-spray`, `takeover`.