# crt.sh

Certificate Transparency log search. No API key needed. Finds subdomains from TLS certificates.

```json
{"tool": "crtsh_domain", "args": {"domain": "target.com"}}
```

Returns up to 200 unique subdomains.