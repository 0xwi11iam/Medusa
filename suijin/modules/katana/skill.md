# Katana

> [warn] **LONG-RUNNING** on large sites — use `"background": true`

Headless crawler for JS-rendered endpoint discovery.

```json
{"tool": "katana_crawl", "args": {"url": "https://target.com", "depth": 3, "flags": "-js-crawl -silent"}}
```