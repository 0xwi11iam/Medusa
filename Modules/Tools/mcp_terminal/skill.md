# MCP Terminal

Persistent bash shell — maintains working directory, environment variables, and history across calls.

```json
{"tool": "mcp_shell_exec", "args": {"cmd": "whoami; id; uname -a"}}
{"tool": "mcp_shell_cd", "args": {"path": "/tmp"}}
```