# WhatWeb

Technology stack fingerprinting — identifies CMS, frameworks, JS libraries, servers, CDNs.

```json
{"tool": "whatweb_scan", "args": {"url": "https://target.com", "flags": "-a 3 --colour=never --no-errors"}}
```